# Feature inventory and acceptance criteria

## Roles

Scroll Race has no accounts, authentication, authorization, or privileged
controls. Every visitor has the same permissions. The relevant user-facing
roles are stateful personas:

| ID          | Role                                | Acceptance criteria                                                                                                                                                            | Finite risk-based edges                                                                                                                |
| ----------- | ----------------------------------- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------ | -------------------------------------------------------------------------------------------------------------------------------------- |
| R-NEW       | New anonymous racer                 | Sees no invented PB, can select an event, race, save with a name or generated bib name, and share.                                                                             | Empty local storage; empty name; storage unavailable; first eligible result; first wind-assisted result.                               |
| R-RETURN    | Returning racer                     | Stored event, scale, mute preference, PB, device boards, streak, run count, and name load without mixing event boards.                                                         | All four boards at cap; legacy rows; malformed stored settings; lapsed streak; auto-save; corrected local name.                        |
| R-CHALLENGE | Challenged racer                    | A valid link pins its event and target, shows the challenger, supplies a pace ghost, and clears only after an eligible win is saved or the player deliberately changes events. | Legacy link without event; minimum eligible target; oversized target; unsafe `by`; same-event click; event change; win/loss/dead heat. |
| R-DEGRADED  | Offline or reduced-capability racer | The local game remains usable when the board, clipboard, native share, audio, haptics, motion, or browser storage is unavailable.                                              | HTTP failure; empty/malformed board; rejected native share; rejected clipboard; reduced motion; storage exceptions.                    |

## Routes

| ID    | Route                                           | Acceptance criteria                                                                                                                     | Finite risk-based edges                                                                                                         |
| ----- | ----------------------------------------------- | --------------------------------------------------------------------------------------------------------------------------------------- | ------------------------------------------------------------------------------------------------------------------------------- |
| RT-01 | `GET /`                                         | Returns the production shell, metadata, manifest link, one interactive race, and no route error.                                        | Fresh/returning storage; mobile/desktop; JS hydration; external font failure.                                                   |
| RT-02 | `GET /?beat=&event=&by=`                        | Strictly parses sanctioned events and eligible integer times, sanitizes the challenger, and keeps the event pinned while active.        | Missing, blank, decimal, negative, junk, sub-eligible, over-max, legacy no-event, unknown event, encoded unsafe name.           |
| RT-03 | `GET /api/leaderboard?event=`                   | Returns only the selected sanctioned event, top 25, accurate nonnegative total, JSON content type, and `no-store`.                      | Empty board; unknown event; 100-row stored cap; corrupt blob; concurrent reads.                                                 |
| RT-04 | `POST /api/leaderboard`                         | Accepts an eligible sanitized score once per cooldown, returns rank/total, and writes only the context-scoped board outside production. | Bad method/JSON/body size/event/time/splits/device/PPI; wind-assisted floor; rate limit; off-board rank; ties; separate events. |
| RT-05 | `/manifest.json`, `/robots.txt`, `/favicon.ico` | Each asset returns HTTP 200 from the production preview.                                                                                | Cache-independent direct request.                                                                                               |
| RT-06 | Five creator destinations                       | Each opens in a new tab with `rel=noreferrer` and an accessible name.                                                                   | Personal site, GitHub, Instagram, LinkedIn, Discord.                                                                            |

## Controls, displays, and workflows

Every rendered button, input, disclosure, dialog, state, and workflow is listed
below. “Automated” refers to the unit/integration or production-preview
campaign; visual screenshots remain in the evidence directory.

| ID    | User-facing surface                         | Acceptance criteria                                                                                                                                                  | Finite risk-based edges                                                                                               |
| ----- | ------------------------------------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------------------------------------------------------------------------------------------------------------------- |
| UI-01 | Intro `Scroll Race` dialog                  | Blocks page scrolling, has an accessible name and `aria-modal=true`, keeps its own long content scrollable, and hides the inactive course from assistive technology. | 320px height overflow; returning animation; challenge slip; calibration expanded.                                     |
| UI-02 | Event radios: 100, 250, 500, 1000 ft        | Exactly one is checked; click and Arrow keys change it; Home/End jump to bounds; selection persists; course, tagline, board, and stats change together.              | Wrap-around arrows; checked re-click; challenge dismissal; board isolation; max course height.                        |
| UI-03 | Best time, Runs, Streak/Target stats        | Shows event PB or local best, live lifetime run total when available, and either streak or challenge target.                                                         | No PB; world loading/error; 125k+ totals; streak below/above day 3; active challenge.                                 |
| UI-04 | Lapsed-streak notice                        | Appears once for a lost 3+ day streak and clears the stored previous-streak marker.                                                                                  | Current day; yesterday; DST-safe calendar day; below-three lapse.                                                     |
| UI-05 | Start/Beat/Race again button                | Starts at course top, enables audio in the gesture, resets prior result/share/rank state, selects challenge or PB ghost, and opens countdown.                        | New, returning, challenge, post-finish, in-flight prior submit.                                                       |
| UI-06 | Calibrate disclosure button                 | Toggles one panel and exposes `aria-expanded`/`aria-controls`.                                                                                                       | Repeated open/close; narrow mobile; persisted nondefault value.                                                       |
| UI-07 | Calibration range input                     | Changes pixels per inch from 72 through 220 and immediately persists the normalized integer.                                                                         | Min, max, decimal, out of range, malformed stored value.                                                              |
| UI-08 | Calibration number input                    | Mirrors the range and uses the same normalization/persistence boundary.                                                                                              | Empty/zero, decimal, min/max, keyboard entry.                                                                         |
| UI-09 | Calibration Reset button                    | Restores and persists 96 px/in.                                                                                                                                      | Reset from min/max; reload.                                                                                           |
| UI-10 | Intro Sound toggle                          | Reflects sound with `aria-pressed`, persists mute state, and survives reload without blocking play.                                                                  | Muted first load; unavailable AudioContext/storage; rapid toggles.                                                    |
| UI-11 | World board button                          | Selects loading, live, empty, or offline world state without changing the event.                                                                                     | 25-row payload/10 rendered; 125k+ total; invalid total; hostile rows; duplicate ids; wrong-event rows.                |
| UI-12 | This device board button                    | Shows only the selected event, sorted fastest-first, capped at 10, with empty ghost rows when needed.                                                                | Four full boards; ties; legacy rows; unsafe local names; malformed storage.                                           |
| UI-13 | Countdown overlay                           | Announces 3-2-1 assertively, prevents accidental page movement, and launches automatically or on an intentional skip.                                                | Pointer, touchmove, wheel after 250ms, Space, Enter, ArrowDown, PageDown, repeated key.                               |
| UI-14 | Race course                                 | Scales to the selected event/PPI, exposes an event-specific label while active, and stops at the finish.                                                             | 100/250/500/1000; resize/mobile toolbar; zero max scroll; overscroll.                                                 |
| UI-15 | HUD timer and progress                      | Appears only while racing/finished, exposes a timer and progress element, reaches the exact event distance, and does not flood the live region.                      | Resize, finish-between-frames, challenge ghost dot.                                                                   |
| UI-16 | HUD Sound toggle                            | Uses the same persisted mute state and behavior as UI-10.                                                                                                            | Toggle mid-race; missing AudioContext.                                                                                |
| UI-17 | HUD Restart button                          | Returns to top and starts a fresh countdown without leaking result, split, ghost-delta, or submit state.                                                             | Mid-race; finished; challenge; rapid restart.                                                                         |
| UI-18 | PB/challenge ghost and delta                | Uses stored splits when available, linear fallback for legacy data, stays calibrated by percent, and updates direction with hysteresis.                              | Missing splits; plateau splits; ahead/behind boundary; event change.                                                  |
| UI-19 | Milestones and announcements                | Marks 25/50/75%, announces real event feet, emits effects when allowed, and never repeats a passed gate.                                                             | One fling crossing several gates; reduced motion; sound muted.                                                        |
| UI-20 | Finish result panel                         | Focuses `Finished!`, shows exact time, rank/PB/challenge verdict, percentile, and three event-scaled facts.                                                          | First run, PB, close miss, large miss, local record, daily first/best, challenge win/loss/dead heat.                  |
| UI-21 | Share this run button                       | Uses native share when available, treats user cancellation as cancellation, falls back to clipboard, and exposes selectable text when either platform API fails.     | Native success/rejection/AbortError; clipboard success/rejection/throw; encoded name budget; challenge counter-share. |
| UI-22 | Share fallback textarea                     | Is read-only, accessible, four rows high, selects on focus, and contains a complete challenge or self-reporting wind-assisted card.                                  | Eligible/challenge/streak/wind-assisted; no saved name; non-ASCII name.                                               |
| UI-23 | Name input                                  | Accepts up to 18 characters, uses nickname completion hints, and is present only for eligible unsaved/editing results.                                               | Empty; max length; punctuation/unsafe text; existing stored name.                                                     |
| UI-24 | Save time button                            | Saves an eligible run locally, caps/sorts the event board, stores the chosen/generated name, and posts one complete 101-split score worldwide.                       | Empty random bib; local rank outside top 10; API failure; 429 single retry; next race before response.                |
| UI-25 | Not `{name}`? button                        | Opens the correction form and moves focus safely after the prior form unmounts.                                                                                      | Auto-saved returning racer; manually saved new racer; Enter key.                                                      |
| UI-26 | Update name button                          | Updates the just-saved local entry and persisted future name without creating another world submission.                                                              | Empty correction; generated replacement; capped board; missing saved id.                                              |
| UI-27 | Run it back button                          | Starts another countdown and changes copy after three non-PB attempts.                                                                                               | PB reset; three-session misses; challenge retry.                                                                      |
| UI-28 | Finish keyboard shortcuts                   | `R` and Enter restart only from body/finish heading, never steal input/button/link keystrokes, and ignore repeats/composition/modifiers.                             | Focus on input/button/link; Ctrl/Meta/Alt; IME composition.                                                           |
| UI-29 | Back to the start button                    | Restores intro at top without clearing valid persistent data.                                                                                                        | Saved/unsaved; challenge loss; share fallback open.                                                                   |
| UI-30 | Not eligible disabled button                | Replaces saving for wind-assisted results and cannot be activated.                                                                                                   | End-key/teleport; sub-minimum time; share remains available.                                                          |
| UI-31 | Wind-assisted detection and card            | Detects a large per-frame jump or sub-event time floor, excludes PB/streak/boards, and self-reports in shared text.                                                  | Final jump between frames; jank-scaled allowance; every event floor.                                                  |
| UI-32 | Confetti, tape, speed lines, audio, haptics | Enhance eligible play and terminate; all motion-heavy visuals are suppressed under reduced motion.                                                                   | Mid-session preference change; no canvas/audio/vibrate; record vs nonrecord.                                          |
| UI-33 | Leaderboard rows and totals                 | Names, time, gap, device, and country cannot overflow or render hostile/wrong-event/negative data.                                                                   | Empty, 1, 10, 25, 100 stored; duplicate ids; long labels; invalid country; negative/decimal total.                    |
| UI-34 | Responsive layout                           | No document-level horizontal overflow; title and start control remain visible; intro can scroll internally.                                                          | 320x700, 390x844, 1280x800, 1440x900; entrance animation settled.                                                     |

## State model

| State family | Finite states                                                                                     |
| ------------ | ------------------------------------------------------------------------------------------------- |
| Race         | `intro`, `countdown`, `racing`, `finished eligible`, `finished wind-assisted`                     |
| Result save  | `unsaved`, `saved`, `editing name`, `world rank pending`, `world rank ready/error`                |
| Board        | `loading`, `ready empty`, `ready populated`, `error with local fallback`; tab `world` or `device` |
| Challenge    | `absent`, `pending`, `won then cleared on save`, `lost and retained`, `dismissed by event change` |
| Share        | `idle`, `native sheet`, `clipboard copied`, `manual-copy fallback`                                |
| Preferences  | Sound on/off; calibration 72-220; reduced motion on/off                                           |
| Persistence  | Fresh, current, legacy, malformed/unavailable                                                     |

No separate confirmation modal exists. The intro is the only ARIA dialog;
calibration is an inline disclosure, countdown is a blocking overlay, and the
finish panel is an in-course region.
